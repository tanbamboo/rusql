## Goal

`IS NULL` / `IS NOT NULL` in WHERE.

## Depends on

- M20 WHERE comparisons

## Acceptance Criteria

- [ ] `SELECT * FROM t WHERE col IS NULL`
- [ ] Tests + docs

## File Boundaries

- crates/rusql-executor/**

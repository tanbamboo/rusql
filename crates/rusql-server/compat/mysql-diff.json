{
  "protocol_smoke": [
    { "sql": "SELECT 1" },
    { "sql": "select @@version_comment limit 1", "compare_output": false }
  ],
  "suites": [
    {
      "name": "portable_dml",
      "steps": [
        { "sql": "CREATE TABLE md_t (id INT NOT NULL, name VARCHAR(32), PRIMARY KEY (id))" },
        { "sql": "INSERT INTO md_t (id, name) VALUES (1, 'alice')" },
        { "sql": "INSERT INTO md_t (id, name) VALUES (2, 'bob')" },
        { "sql": "SELECT id, name FROM md_t ORDER BY id" },
        { "sql": "UPDATE md_t SET name = 'Alice' WHERE id = 1" },
        { "sql": "SELECT name FROM md_t WHERE id = 1" },
        { "sql": "DELETE FROM md_t WHERE id = 2" },
        { "sql": "SELECT * FROM md_t ORDER BY id" }
      ]
    },
    {
      "name": "portable_index",
      "steps": [
        { "sql": "CREATE TABLE md_i (id INT PRIMARY KEY, label VARCHAR(16))" },
        { "sql": "CREATE INDEX idx_md_label ON md_i (label)" },
        { "sql": "INSERT INTO md_i VALUES (1, 'x')" },
        { "sql": "INSERT INTO md_i VALUES (2, 'y')" },
        { "sql": "SELECT id FROM md_i WHERE label = 'y'" }
      ]
    },
    {
      "name": "extended_where",
      "steps": [
        { "sql": "CREATE TABLE ew_t (id INT, name VARCHAR(16))" },
        { "sql": "INSERT INTO ew_t VALUES (1, 'alice')" },
        { "sql": "INSERT INTO ew_t VALUES (2, 'bob')" },
        { "sql": "INSERT INTO ew_t VALUES (3, 'carol')" },
        { "sql": "SELECT id FROM ew_t WHERE id = 1 OR id = 3" },
        { "sql": "SELECT name FROM ew_t WHERE name LIKE 'a%'" },
        { "sql": "SELECT id FROM ew_t WHERE id BETWEEN 2 AND 3" },
        { "sql": "SELECT id FROM ew_t WHERE id IN (1, 3)" },
        { "sql": "SELECT id FROM ew_t WHERE id NOT IN (1, 3)" },
        { "sql": "SELECT name FROM ew_t WHERE NOT name LIKE 'a%'" }
      ]
    },
    {
      "name": "multi_schema",
      "steps": [
        { "sql": "CREATE DATABASE app_db" },
        { "sql": "USE app_db" },
        { "sql": "CREATE TABLE t (id INT PRIMARY KEY)" },
        { "sql": "INSERT INTO t VALUES (1)" },
        { "sql": "SELECT id FROM t" },
        { "sql": "SHOW DATABASES", "compare_output": false },
        { "sql": "DROP TABLE t" },
        { "sql": "DROP DATABASE app_db" }
      ]
    },
    {
      "name": "auto_increment",
      "steps": [
        { "sql": "CREATE TABLE ai_t (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(16))" },
        { "sql": "INSERT INTO ai_t (name) VALUES ('alice')" },
        { "sql": "INSERT INTO ai_t (name) VALUES ('bob')" },
        { "sql": "SELECT id, name FROM ai_t ORDER BY id" },
        { "sql": "SHOW CREATE TABLE ai_t", "compare_output": false }
      ]
    },
    {
      "name": "extended_types",
      "steps": [
        { "sql": "CREATE TABLE types_t (id INT PRIMARY KEY, amount DECIMAL(10,2), created_at DATETIME, payload JSON, note TEXT)" },
        { "sql": "INSERT INTO types_t VALUES (1, 99.50, '2026-01-15 10:30:00', '{\"k\":1}', 'hello')" },
        { "sql": "SELECT id, amount, created_at, payload, note FROM types_t" },
        { "sql": "DESCRIBE types_t", "compare_output": false }
      ]
    },
    {
      "name": "outer_join",
      "steps": [
        { "sql": "CREATE TABLE oj_a (id INT PRIMARY KEY, name VARCHAR(16))" },
        { "sql": "CREATE TABLE oj_b (id INT PRIMARY KEY, a_id INT, label VARCHAR(16))" },
        { "sql": "INSERT INTO oj_a VALUES (1, 'alice'), (2, 'bob')" },
        { "sql": "INSERT INTO oj_b VALUES (10, 1, 'x')" },
        { "sql": "SELECT oj_a.id, oj_a.name, oj_b.label FROM oj_a LEFT JOIN oj_b ON oj_a.id = oj_b.a_id ORDER BY oj_a.id" },
        { "sql": "SELECT oj_a.id, oj_b.label FROM oj_a RIGHT JOIN oj_b ON oj_a.id = oj_b.a_id ORDER BY oj_b.id" }
      ]
    },
    {
      "name": "alter_table_extended",
      "steps": [
        { "sql": "CREATE TABLE alt_t (id INT PRIMARY KEY, extra VARCHAR(8), name VARCHAR(16))" },
        { "sql": "INSERT INTO alt_t VALUES (1, 'x', 'alice')" },
        { "sql": "ALTER TABLE alt_t DROP COLUMN extra" },
        { "sql": "ALTER TABLE alt_t RENAME COLUMN name TO label" },
        { "sql": "ALTER TABLE alt_t MODIFY COLUMN label VARCHAR(32) NOT NULL" },
        { "sql": "SELECT id, label FROM alt_t" },
        { "sql": "ALTER TABLE alt_t RENAME TO alt_t2" },
        { "sql": "SELECT id, label FROM alt_t2" }
      ]
    }
  ]
}
